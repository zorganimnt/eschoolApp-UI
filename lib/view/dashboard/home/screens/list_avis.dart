
import 'package:flutter/material.dart';

class ListAvis extends StatelessWidget {
  const ListAvis({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("List avis"),
    );
  }
}
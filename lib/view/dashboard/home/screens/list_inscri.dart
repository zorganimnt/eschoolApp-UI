import 'package:flutter/material.dart';

class ListInscri extends StatelessWidget {
  const ListInscri({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("List Inscri"),
    );
  }
}
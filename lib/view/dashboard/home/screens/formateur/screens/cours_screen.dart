import 'package:flutter/material.dart';

class CourScreen extends StatelessWidget {
  const CourScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text("cours"),);
  }
}